(function() {
  $('#modal-change-password').remove();
  $('body').append('<%= j(render 'modal_change_password', user: @user) %>');
  var $modal = $('#modal-change-password');
  $modal.modal('show');
})();
