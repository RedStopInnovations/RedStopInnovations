(function() {
  <% if @user.errors.any? %>
    $('#modal-change-password').html($('<%= j(render 'modal_change_password', user: @user) %>').html());
  <% else %>
    $('#modal-change-password').modal('hide');
    Flash.success('The password has been successfully updated.');
  <% end %>
})();
