<div class="side_nav">
  <div class="navbar navbar-default" role="navigation">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".sidebar-navbar-collapse">
        <span class="green_menubar"></span>
      </button>
    </div>

    <div class="navbar-collapse collapse sidebar-navbar-collapse">
      <ul class="nav navbar-nav">
        <li class=<%= (params[:controller] == 'admin/patients' && ['index', 'edit', 'show'].include?(params[:action])) ? 'active' : '' %>>
          <a href="<%= admin_patient_path(@patient) %>">Patient details</a>
        </li>
        <li class=<%= (params[:controller] == 'admin/patients' && ['invoices'].include?(params[:action])) ? 'active' : '' %>>
          <a href="<%= invoices_admin_patient_path(@patient) %>">Invoices</a>
        </li>
        <li class=<%= (params[:controller] == 'admin/patients' && ['payments'].include?(params[:action])) ? 'active' : '' %>>
          <a href="<%= payments_admin_patient_path(@patient) %>">Payments</a>
        </li>
        <li class=<%= (params[:controller] == 'admin/patients' && ['appointments'].include?(params[:action])) ? 'active' : '' %>>
          <a href="<%= appointments_admin_patient_path(@patient) %>">Appointments</a>
        </li>
      </ul>
    </div>
  </div>
</div>